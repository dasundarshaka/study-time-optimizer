services:
  - type: web
    name: simplex-backend
    runtime: java
    rootDir: Backend
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/simplex-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: "21"
